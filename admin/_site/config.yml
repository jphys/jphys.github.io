# config.yml

backend:
  name: github
  repo: jphys/jphys.github.io
  branch: main
  site_domain: cocky-elion-2ab0cd.netlify.com # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    fields:
      - { name: Title }
